
name: run-docker

on:
  push:
    branches: [ main ]

  workflow_dispatch:
jobs:
  myjob:
    runs-on: ubuntu-latest
    steps:
      - name: login
        run: docker login -u $GITHUB_ACTOR -p ${{secrets.GITHUB_TOKEN}} docker.pkg.github.com
      - name: Run my simple image
        uses: docker://raiyanyahya/simple:latest
        with:
          #entrypoint: /opt/box/box
          args: 'input'
      
